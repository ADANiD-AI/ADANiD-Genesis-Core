{
  "name": "adanid-genesis-core",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"  // Changed from 18 to 20 to fix EBADENGINE warning
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^11.5.0",
    "firebase-functions": "^4.2.1",
    "express": "^4.18.2",
    "crypto-js": "^4.2.0",
    "mongoose": "^8.0.0" // Required for mongoConnect and User model
  },
  "private": true
}
